{
  "name": "@halal-tinder/config",
  "version": "0.1.0",
  "description": "Shared configuration files",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "eslint.js", "prettier.js"],
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
